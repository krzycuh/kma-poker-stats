# Poker Stats - Production Environment Configuration Template
# Copy this file to .env.production and fill in the values
# NEVER commit .env.production to version control!

# ============================================
# Application Info
# ============================================
VERSION=1.0.0
BUILD_DATE=$(date -u +'%Y-%m-%dT%H:%M:%SZ')
VCS_REF=$(git rev-parse --short HEAD)

# ============================================
# Data Storage Path (for Raspberry Pi SSD)
# ============================================
# Path to external SSD or storage device
DATA_PATH=/mnt/pokerstats-data

# ============================================
# PostgreSQL Configuration
# ============================================
POSTGRES_DB=pokerstats
POSTGRES_USER=pokerstats
# SECURITY: Generate a strong password (min 20 characters)
# Use: openssl rand -base64 32
POSTGRES_PASSWORD=CHANGE_ME_GENERATE_STRONG_PASSWORD

# ============================================
# Redis Configuration
# ============================================
# SECURITY: Generate a strong password
# Use: openssl rand -base64 32
REDIS_PASSWORD=CHANGE_ME_GENERATE_STRONG_PASSWORD

# ============================================
# JWT Configuration
# ============================================
# SECURITY: Generate a strong secret (min 32 characters)
# Use: openssl rand -base64 64
JWT_SECRET=CHANGE_ME_GENERATE_STRONG_JWT_SECRET_MIN_32_CHARS
# Token expiration in milliseconds
JWT_EXPIRATION=86400000        # 24 hours
JWT_REFRESH_EXPIRATION=604800000  # 7 days

# ============================================
# CORS Configuration
# ============================================
# Set to your domain(s), comma-separated
# Example: https://pokerstats.yourdomain.com,https://www.pokerstats.yourdomain.com
CORS_ALLOWED_ORIGINS=https://pokerstats.local

# ============================================
# API Configuration
# ============================================
# Public URL for the API (used by frontend)
VITE_API_URL=https://pokerstats.local/api

# ============================================
# Email Configuration (for notifications)
# ============================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-specific-password
SMTP_FROM=noreply@pokerstats.local

# ============================================
# Backup Configuration
# ============================================
BACKUP_RETENTION_DAYS=30
BACKUP_PATH=/mnt/pokerstats-data/backups

# ============================================
# Logging
# ============================================
LOG_LEVEL=INFO

# ============================================
# Security Checklist
# ============================================
# [ ] Changed all default passwords
# [ ] Generated strong JWT secret (min 64 chars)
# [ ] Set correct CORS origins
# [ ] Configured external SSD path
# [ ] Set up SSL certificates
# [ ] Verified firewall rules
# [ ] Enabled automatic backups
# [ ] Configured monitoring alerts
